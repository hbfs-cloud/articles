{"queries":[{"id":"region_summary","compiledQueryString":"select\n    count(*) as nb_stocks,\n    sum(market_cap) as total_mcap,\n    round(avg(pe_forward), 1) as avg_pe_forward,\n    round(avg(dividend_yield), 2) as avg_div_yield,\n    round(avg(change_pct), 2) as avg_change\nfrom market.stocks\nwhere region like '${inputs.region_select.value}'","inputQueryString":"select\n    count(*) as nb_stocks,\n    sum(market_cap) as total_mcap,\n    round(avg(pe_forward), 1) as avg_pe_forward,\n    round(avg(dividend_yield), 2) as avg_div_yield,\n    round(avg(change_pct), 2) as avg_change\nfrom market.stocks\nwhere region like '${inputs.region_select.value}'","compiled":false,"inline":true},{"id":"by_country","compiledQueryString":"select\n    country,\n    count(*) as nb_stocks,\n    sum(market_cap) as total_mcap,\n    round(avg(pe_forward), 1) as avg_pe,\n    round(avg(dividend_yield), 2) as avg_div_yield,\n    round(avg(change_pct), 2) as avg_change\nfrom market.stocks\nwhere region like '${inputs.region_select.value}'\ngroup by country\norder by total_mcap desc","inputQueryString":"select\n    country,\n    count(*) as nb_stocks,\n    sum(market_cap) as total_mcap,\n    round(avg(pe_forward), 1) as avg_pe,\n    round(avg(dividend_yield), 2) as avg_div_yield,\n    round(avg(change_pct), 2) as avg_change\nfrom market.stocks\nwhere region like '${inputs.region_select.value}'\ngroup by country\norder by total_mcap desc","compiled":false,"inline":true},{"id":"top10_by_country","compiledQueryString":"select\n    country,\n    sum(market_cap) as total_mcap\nfrom market.stocks\nwhere region like '${inputs.region_select.value}'\ngroup by country\norder by total_mcap desc\nlimit 10","inputQueryString":"select\n    country,\n    sum(market_cap) as total_mcap\nfrom market.stocks\nwhere region like '${inputs.region_select.value}'\ngroup by country\norder by total_mcap desc\nlimit 10","compiled":false,"inline":true},{"id":"sector_breakdown","compiledQueryString":"select\n    sector,\n    count(*) as nb_stocks,\n    sum(market_cap) as total_mcap\nfrom market.stocks\nwhere region like '${inputs.region_select.value}'\ngroup by sector\norder by total_mcap desc","inputQueryString":"select\n    sector,\n    count(*) as nb_stocks,\n    sum(market_cap) as total_mcap\nfrom market.stocks\nwhere region like '${inputs.region_select.value}'\ngroup by sector\norder by total_mcap desc","compiled":false,"inline":true},{"id":"region_top_stocks","compiledQueryString":"select\n    symbol,\n    name,\n    price,\n    change_pct,\n    market_cap,\n    pe_forward,\n    dividend_yield,\n    sector,\n    country\nfrom market.stocks\nwhere region like '${inputs.region_select.value}'\norder by market_cap desc\nlimit 20","inputQueryString":"select\n    symbol,\n    name,\n    price,\n    change_pct,\n    market_cap,\n    pe_forward,\n    dividend_yield,\n    sector,\n    country\nfrom market.stocks\nwhere region like '${inputs.region_select.value}'\norder by market_cap desc\nlimit 20","compiled":false,"inline":true},{"id":"region_stocks_all","compiledQueryString":"select\n    symbol,\n    name,\n    price,\n    change_pct,\n    market_cap,\n    pe_forward,\n    dividend_yield,\n    sector,\n    region,\n    country\nfrom market.stocks\nwhere region like '${inputs.region_select.value}'\norder by market_cap desc","inputQueryString":"select\n    symbol,\n    name,\n    price,\n    change_pct,\n    market_cap,\n    pe_forward,\n    dividend_yield,\n    sector,\n    region,\n    country\nfrom market.stocks\nwhere region like '${inputs.region_select.value}'\norder by market_cap desc","compiled":false,"inline":true}]}