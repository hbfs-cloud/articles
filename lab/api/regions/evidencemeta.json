{"queries":[{"id":"sankey_flows","compiledQueryString":"select\n    region as source,\n    sector as target,\n    count(*) as amount\nfrom market.stocks\nwhere region is not null and sector is not null\ngroup by region, sector\norder by amount desc","inputQueryString":"select\n    region as source,\n    sector as target,\n    count(*) as amount\nfrom market.stocks\nwhere region is not null and sector is not null\ngroup by region, sector\norder by amount desc","compiled":false,"inline":true},{"id":"heatmap_region_sector","compiledQueryString":"select\n    region,\n    sector,\n    round(avg(change_pct), 2) as avg_change\nfrom market.stocks\nwhere region is not null and sector is not null\ngroup by region, sector\norder by region, sector","inputQueryString":"select\n    region,\n    sector,\n    round(avg(change_pct), 2) as avg_change\nfrom market.stocks\nwhere region is not null and sector is not null\ngroup by region, sector\norder by region, sector","compiled":false,"inline":true},{"id":"mcap_by_region_sector","compiledQueryString":"select\n    region,\n    sector,\n    sum(market_cap) as total_mcap\nfrom market.stocks\nwhere region is not null and sector is not null\ngroup by region, sector\norder by region, total_mcap desc","inputQueryString":"select\n    region,\n    sector,\n    sum(market_cap) as total_mcap\nfrom market.stocks\nwhere region is not null and sector is not null\ngroup by region, sector\norder by region, total_mcap desc","compiled":false,"inline":true},{"id":"country_summary","compiledQueryString":"select\n    country,\n    count(*) as nb_stocks,\n    sum(market_cap) as total_mcap,\n    round(avg(pe_forward), 1) as avg_pe,\n    round(avg(dividend_yield), 2) as avg_div,\n    round(avg(change_pct), 2) as avg_change\nfrom market.stocks\nwhere country is not null\ngroup by country\norder by total_mcap desc","inputQueryString":"select\n    country,\n    count(*) as nb_stocks,\n    sum(market_cap) as total_mcap,\n    round(avg(pe_forward), 1) as avg_pe,\n    round(avg(dividend_yield), 2) as avg_div,\n    round(avg(change_pct), 2) as avg_change\nfrom market.stocks\nwhere country is not null\ngroup by country\norder by total_mcap desc","compiled":false,"inline":true},{"id":"top15_countries_mcap","compiledQueryString":"select\n    country,\n    sum(market_cap) as total_mcap\nfrom market.stocks\nwhere country is not null\ngroup by country\norder by total_mcap desc\nlimit 15","inputQueryString":"select\n    country,\n    sum(market_cap) as total_mcap\nfrom market.stocks\nwhere country is not null\ngroup by country\norder by total_mcap desc\nlimit 15","compiled":false,"inline":true},{"id":"funnel_region","compiledQueryString":"select\n    region,\n    count(*) as nb_stocks\nfrom market.stocks\nwhere region is not null\ngroup by region\norder by nb_stocks desc","inputQueryString":"select\n    region,\n    count(*) as nb_stocks\nfrom market.stocks\nwhere region is not null\ngroup by region\norder by nb_stocks desc","compiled":false,"inline":true},{"id":"region_overview","compiledQueryString":"select\n    region,\n    count(*) as nb_stocks,\n    sum(market_cap) as total_mcap,\n    round(avg(pe_forward), 1) as avg_pe,\n    round(avg(dividend_yield), 2) as avg_div,\n    round(avg(change_pct), 2) as avg_change\nfrom market.stocks\nwhere region is not null\ngroup by region\norder by total_mcap desc","inputQueryString":"select\n    region,\n    count(*) as nb_stocks,\n    sum(market_cap) as total_mcap,\n    round(avg(pe_forward), 1) as avg_pe,\n    round(avg(dividend_yield), 2) as avg_div,\n    round(avg(change_pct), 2) as avg_change\nfrom market.stocks\nwhere region is not null\ngroup by region\norder by total_mcap desc","compiled":false,"inline":true},{"id":"region_list_btn","compiledQueryString":"select distinct region as value, region as label\nfrom market.stocks\nwhere region is not null\norder by region","inputQueryString":"select distinct region as value, region as label\nfrom market.stocks\nwhere region is not null\norder by region","compiled":false,"inline":true},{"id":"rg_stats","compiledQueryString":"select\n    count(*) as nb_stocks,\n    sum(market_cap) as total_mcap,\n    round(avg(pe_forward), 1) as avg_pe,\n    round(avg(dividend_yield), 2) as avg_div,\n    round(avg(change_pct), 2) as avg_change\nfrom market.stocks\nwhere region = '${inputs.region_select.value}'","inputQueryString":"select\n    count(*) as nb_stocks,\n    sum(market_cap) as total_mcap,\n    round(avg(pe_forward), 1) as avg_pe,\n    round(avg(dividend_yield), 2) as avg_div,\n    round(avg(change_pct), 2) as avg_change\nfrom market.stocks\nwhere region = '${inputs.region_select.value}'","compiled":false,"inline":true},{"id":"rg_sector_breakdown","compiledQueryString":"select\n    sector,\n    count(*) as nb_stocks,\n    sum(market_cap) as total_mcap,\n    round(avg(pe_forward), 1) as avg_pe,\n    round(avg(change_pct), 2) as avg_change\nfrom market.stocks\nwhere region = '${inputs.region_select.value}'\n  and sector is not null\ngroup by sector\norder by total_mcap desc","inputQueryString":"select\n    sector,\n    count(*) as nb_stocks,\n    sum(market_cap) as total_mcap,\n    round(avg(pe_forward), 1) as avg_pe,\n    round(avg(change_pct), 2) as avg_change\nfrom market.stocks\nwhere region = '${inputs.region_select.value}'\n  and sector is not null\ngroup by sector\norder by total_mcap desc","compiled":false,"inline":true},{"id":"rg_sector_mcap","compiledQueryString":"select\n    sector,\n    sum(market_cap) as total_mcap\nfrom market.stocks\nwhere region = '${inputs.region_select.value}'\n  and sector is not null\ngroup by sector\norder by total_mcap desc","inputQueryString":"select\n    sector,\n    sum(market_cap) as total_mcap\nfrom market.stocks\nwhere region = '${inputs.region_select.value}'\n  and sector is not null\ngroup by sector\norder by total_mcap desc","compiled":false,"inline":true},{"id":"rg_top_stocks","compiledQueryString":"select\n    symbol,\n    name,\n    price,\n    change_pct,\n    market_cap,\n    pe_forward,\n    dividend_yield,\n    revenue_growth,\n    profit_margin,\n    sector,\n    country\nfrom market.stocks\nwhere region = '${inputs.region_select.value}'\norder by market_cap desc\nlimit 20","inputQueryString":"select\n    symbol,\n    name,\n    price,\n    change_pct,\n    market_cap,\n    pe_forward,\n    dividend_yield,\n    revenue_growth,\n    profit_margin,\n    sector,\n    country\nfrom market.stocks\nwhere region = '${inputs.region_select.value}'\norder by market_cap desc\nlimit 20","compiled":false,"inline":true}]}