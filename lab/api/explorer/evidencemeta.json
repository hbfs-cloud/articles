{"queries":[{"id":"sector_list","compiledQueryString":"select distinct sector as value, sector as label\nfrom market.stocks\norder by sector","inputQueryString":"select distinct sector as value, sector as label\nfrom market.stocks\norder by sector","compiled":false,"inline":true},{"id":"filtered_stocks","compiledQueryString":"select\n    symbol,\n    name,\n    price,\n    change_pct,\n    volume,\n    market_cap,\n    pe_forward,\n    dividend_yield,\n    sector,\n    region,\n    country\nfrom market.stocks\nwhere sector like '${inputs.sector_filter.value}'\n  and region like '${inputs.region_filter.value}'\norder by market_cap desc","inputQueryString":"select\n    symbol,\n    name,\n    price,\n    change_pct,\n    volume,\n    market_cap,\n    pe_forward,\n    dividend_yield,\n    sector,\n    region,\n    country\nfrom market.stocks\nwhere sector like '${inputs.sector_filter.value}'\n  and region like '${inputs.region_filter.value}'\norder by market_cap desc","compiled":false,"inline":true},{"id":"scatter_data","compiledQueryString":"select\n    symbol,\n    name,\n    pe_forward,\n    change_pct,\n    market_cap,\n    sector,\n    region\nfrom market.stocks\nwhere sector like '${inputs.sector_filter.value}'\n  and region like '${inputs.region_filter.value}'\n  and pe_forward is not null\n  and pe_forward > 0\n  and pe_forward < 200","inputQueryString":"select\n    symbol,\n    name,\n    pe_forward,\n    change_pct,\n    market_cap,\n    sector,\n    region\nfrom market.stocks\nwhere sector like '${inputs.sector_filter.value}'\n  and region like '${inputs.region_filter.value}'\n  and pe_forward is not null\n  and pe_forward > 0\n  and pe_forward < 200","compiled":false,"inline":true},{"id":"filtered_count","compiledQueryString":"select count(*) as nb from market.stocks\nwhere sector like '${inputs.sector_filter.value}'\n  and region like '${inputs.region_filter.value}'","inputQueryString":"select count(*) as nb from market.stocks\nwhere sector like '${inputs.sector_filter.value}'\n  and region like '${inputs.region_filter.value}'","compiled":false,"inline":true},{"id":"filtered_avg_pe","compiledQueryString":"select round(avg(pe_forward), 1) as avg_pe\nfrom market.stocks\nwhere sector like '${inputs.sector_filter.value}'\n  and region like '${inputs.region_filter.value}'\n  and pe_forward is not null\n  and pe_forward > 0","inputQueryString":"select round(avg(pe_forward), 1) as avg_pe\nfrom market.stocks\nwhere sector like '${inputs.sector_filter.value}'\n  and region like '${inputs.region_filter.value}'\n  and pe_forward is not null\n  and pe_forward > 0","compiled":false,"inline":true},{"id":"filtered_total_mcap","compiledQueryString":"select sum(market_cap) as total_mcap\nfrom market.stocks\nwhere sector like '${inputs.sector_filter.value}'\n  and region like '${inputs.region_filter.value}'","inputQueryString":"select sum(market_cap) as total_mcap\nfrom market.stocks\nwhere sector like '${inputs.sector_filter.value}'\n  and region like '${inputs.region_filter.value}'","compiled":false,"inline":true}]}