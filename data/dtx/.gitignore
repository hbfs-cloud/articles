# dtx-scan staging. The cloud pipeline (no systematic-tss, no network) READS committed staging;
# a systematic-tss+network host (dev nightly / ser) refreshes + commits it BEFORE the 23h routine.
#
# dtx MCP v15 cut-over (2026-07-13): only the 6 cost-honest viable strategies are wired
# (DTX_STAGING_MAP), so only those are versioned. Everything else (legacy scripted books +
# unwired portfolios + machine-local engine state) stays ignored — regenerable byproducts.
*.json
state/
!us_highvol.json
!etf_us.json
!stockbox_pit.json
!book_honest.json
!hvep.json
!ep.json
