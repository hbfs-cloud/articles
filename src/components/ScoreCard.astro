---
/**
 * ScoreCard — Grade display (A+ to D).
 */
interface Props {
  grade: string;
  label?: string;
}

const { grade, label = 'Grade DailyTickers' } = Astro.props;
---

<div class="score-card">
  <div class="score-value">{grade}</div>
  <div class="score-label">{label}</div>
</div>
