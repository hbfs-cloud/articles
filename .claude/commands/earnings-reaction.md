# /earnings-reaction — Jouer les résultats (pré à surveiller + post-earnings drift) + bilan

Exécute le skill **earnings-reaction** : lis `.claude/skills/earnings-reaction.md` et suis-le **EXACTEMENT**.

Bilan → calendrier (GetEarningsCalendarFiltered) → PRÉ (qualité du setup : beats, guidance, révisions — à surveiller, pas d'entrée aveugle) → POST (drift sur beat + guidance relevée + gap tenu = gap-and-go, niveaux) → anti-dilution → cohérence/régime → digest.

## Arguments
`$ARGUMENTS` — `à venir 5j` / `post-résultats` / un secteur (défaut : les deux volets, US). `ne poste pas` = montrer sans envoyer.

Garde-fous : zéro hallucination (dates/EPS/guidance via MCP), gap-risk explicite + taille réduite, idées ≠ données desk, format html `<b>`, envoi sur demande.
