# /swing-signals — Signaux swing court terme (5-10 séances) + bilan + digest Telegram

Exécute le skill **swing-signals** : lis `.claude/skills/swing-signals.md` et suis-le **EXACTEMENT**.

Rappel plans passés → revalidation au spot (MCP) → screener 2 passes (+ presets `config/signal-presets.yaml`) → validation niveaux (R/R≥1.5, earnings ±3j, dilution) → cohérence Strategist → digest Telegram (thèse + Achète si/Skip si + Stop/Cibles $ et %). **Envoi Telegram sur demande explicite uniquement.**

## Arguments
`$ARGUMENTS` — univers/secteur/contrainte (défaut : US large/mid cap). `ne poste pas` = montrer sans envoyer.

Garde-fous : zéro hallucination (tout via MCP), idées ≠ données desk (alias public), format html `<b>`.
