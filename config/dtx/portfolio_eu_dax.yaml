# GENERATED by anneal_optimizer (seed=42) — pareto_defensive
# diff vs baseline: rotation_lookback_days: 84→63; rotation_top_k: 4→3; regime_scale_ma_days: 0→150; regime_scale_fraction: 0.4→0.5; rotation_cooldown_days: 0→21
#
# ⚠️ COST-HONEST CORRECTION (2026-07-12): the commit e32d9bdb message claimed 24.4%/DD16.2/S1.22,
# but that was measured WITHOUT costs. Re-measured cost-honest (TOTAL_RETURN=1, FROZEN_BARS_DATE=
# 2026-07-08, COST_SPREAD_BPS=10, -max-participation 0.1), standalone 2021-01-01..2026-06-30:
#   CAGR ~23% / MaxDD ~28% / Sharpe 0.82 / R2 0.95  → SUB-VIABLE standalone (Sharpe<1, CAGR<DD).
# eu_dax is NOT a standalone-deployable sleeve. It EARNS its slot only as a DIVERSIFIER inside a
# book (arsenal_mindd 20%): decorrelation from US momentum cuts book DD, even though its own edge
# is thin. Do not deploy eu_dax alone.
portfolios:
- id: eu_dax
  name: EU DAX Top-5 Momentum Rotation
  currency: EUR
  initial_capital: 100000
  allocations:
  - name: eu_dax
    region: DE
    currency: EUR
    percent: 100
    broker: saxo
    strategy: index-rotation
    stocks: true
    etfs: false
    pure: true
    whitelist: SAP.DE,SIE.DE,DTE.DE,ALV.DE,RHM.DE,MUV2.DE,IFX.DE,DHL.DE,DBK.DE,BMW.DE,MRK.DE,DB1.DE,VOW3.DE,BAS.DE,SHL.DE,RWE.DE,CBK.DE,HEI.DE,HNR1.DE,HOT.DE,TLX.DE,HEN.DE,FRE.DE,ADS.DE,VNA.DE,MTX.DE,UN0.DE,KBX.DE,BEI.DE,SRT.DE,CON.DE,DWS.DE,FME.DE,G1A.DE,PAH3.DE
    regime_tickers:
      index: ^STOXX50E
      volatility: ^VIX
    scanner_filters:
      params:
        rotation_lookback_days: 63
        rotation_top_k: 3
        rotation_rebalance_days: 21
        regime_ma_days: 200
        regime_scale_fraction: 0.5
        rotation_abs_filter: 1
        regime_scale_ma_days: 150
        rotation_cooldown_days: 21
