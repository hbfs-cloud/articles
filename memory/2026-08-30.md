# 2026-08-30

- A file such as `analyses/<TICKER>/_data/fundamentals.json` is a current alias, not an archive. It is acceptable only with a sibling harness containing collection time, reference close, plan hash, source hashes and source temporal metadata. Primary evidence and published versions remain dated or content-addressed.
- Hardened `$analyse` to require the full visible marketdata surface plus leaders, peers, sector benchmarks and first/second-order blast radius. Correlation is not causality; residualize against QQQ and expose R-squared/confidence.
- AVGO review lesson: distinguish Broadcom's 35B initial Apollo/Blackstone financing-platform tranche from the separate 29B maximum conditional lease backstop in the 10-Q. Never add the amounts automatically.
- Preserve the original Finviz chart. Cache it with the reference date and display it at native resolution rather than stretching it.
- Current marketdata owner audit is `.claude/prompts/marketdata-owner-audit-20260830.md`; it contains reproducible inputs, expected/current behavior and acceptance criteria. Never print MCP credentials or tokens.
