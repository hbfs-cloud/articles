# 2026-09-01

- Scanner notification incident: the daily card mixed `scanner-metrics.json` (205 raw editorial ideas)
  with `scanner-positions.json` (four positions filtered across modes), so the performance block and open
  positions described different portfolios. The card must never combine those scopes.
- The corrected card uses the exact-close `best` replay for historical backtest metrics and does not show
  live positions unless a single certified book source proves both positions and performance for the same
  mode and vintage.
- Generated scanner images must embed the real `/logo.svg`, use the DailyTickers name, and keep Finviz as
  the established chart source. Finviz currently redirects through multiple hops; fetchers must follow the
  full chain and embed the final image before screenshot capture.
