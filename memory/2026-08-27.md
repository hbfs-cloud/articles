# 2026-08-27

- Ajout d'une couche de compatibilité Codex pour le projet DailyTickers Articles, historiquement structuré
  autour de Claude Code : `AGENTS.md` charge désormais `CODEX.md`, `CLAUDE.md`, et les `CLAUDE.md` locaux
  pertinents; `CODEX.md` précise comment traduire commandes, skills et mémoires `.claude/` pour Codex.
- Report de la config MCP Claude projet `.mcp.json` vers Codex dans `.codex/config.toml` : marketdata,
  systematic, memory, notification, simulator, substack. Zéro secret dans le fichier; OAuth/utilisateur à
  gérer côté Codex.
- Note compatibilité commandes : Codex ne transforme pas `.claude/commands/*.md` en entrées natives du
  menu slash. Les commandes tapées par l'utilisateur (`/scanner`, `/daily`, `/weekly`, etc.) doivent être
  routées vers le fichier `.claude/commands/<name>.md` correspondant.
- Création effective de `.codex/skills/dailytickers-commands/SKILL.md`, skill Codex projet qui mappe les
  anciennes commandes Claude vers leurs runbooks `.claude/commands/*.md`.
- Ajout de `.codex/skills/scanner/SKILL.md` comme raccourci Codex dédié : `$scanner` doit suivre
  `.claude/commands/scanner.md` + `.claude/skills/scanner-pipeline.md`.
